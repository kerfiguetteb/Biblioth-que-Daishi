{% extends 'base.html.twig' %}

{% block title %}Emprunt{% endblock %}

{% block body %}
    <h1>Emprunt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emprunt.id }}</td>
            </tr>
            <tr>
                <th>Date_emprunt</th>
                <td>{{ emprunt.dateEmprunt ? emprunt.dateEmprunt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_retour</th>
                <td>{{ emprunt.dateRetour ? emprunt.dateRetour|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>livre</th>
                <td>{{ emprunt.livre.titre }} {{ emprunt.livre.codeIsbn }} </td>      
            </tr>
            <tr>
                <th>emprunteur</th>
                <td>{{ emprunt.emprunteur.nom }} {{ emprunt.emprunteur.prenom }} </td>      
            </tr>

        </tbody>
    </table>

    <a href="{{ path('emprunt_index') }}">back to list</a>

    <a href="{{ path('emprunt_edit', {'id': emprunt.id}) }}">edit</a>

    {{ include('emprunt/_delete_form.html.twig') }}
{% endblock %}
